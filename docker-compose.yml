services:
  docs:
    image: ghcr.io/${GITHUB_REPO:-your-org/Modex-api-doc}:${IMAGE_TAG:-latest}
    container_name: Modex-docs
    restart: unless-stopped
    ports:
      - "${DOCS_PORT:-3000}:80"
